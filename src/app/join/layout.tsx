import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Вступить в Комитет",
  description: "Подайте заявку на вступление в Комитет «ОПОРЫ РОССИИ» по развитию национального рынка труда и мониторингу миграционных процессов.",
};

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return children;
}
